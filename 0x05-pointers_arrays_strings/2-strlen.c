#include "main.h"

/**
 * _strien - funtion that returns the length of a string
 * @s: this is the input string
 * Return: Length of the string
 */
int _strien(char *s)
{
	int index;

	for (index = 0; s[index] != '\0' : index++)

	return (index);
}
